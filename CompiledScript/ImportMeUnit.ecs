

// Set a global variable:
set(definedInUnit "I am from the imported unit")

// Define a function to use anywhere we imported it:
def (
    sayHelloUnit () (
        print("Hello!")
        print("    From the imported unit!")
    )
)
