// This file is valid, but has no indenting
// we use it to demonstrate auto-indenting


set(i 10)
while ( not-equal(i 0)
if ( %(i)
print(i)
)
set(i -(i 1))
)

// function defs
def (
timesTwo (x) (
return( *(2 x) )
)
)


def (
ok!(x) (
if ( x
print("You said true")
return()                // no value returned, so it's ok that not all paths have a return.
)
print("You said false")
)
)
