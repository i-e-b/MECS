set(i 0)
set(t 0)
while ( <(i 64)
    set(j 0)

    while ( <(j 48)
        set(x 0)
        set(y 0)
        set(z 1)
        while ( <(z 10)
            //print( z )
            
            set(x get(y))
            set(y get(z))
            set(z +(x y))
            set(t +(t 1))
        )
        set(j +(j 1))
    )
	//print( i )
    set(i +(i 1))
)
print( "final value = " z )
print("ran " t " iterations")