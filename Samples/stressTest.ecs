

print("running stress test")
set(i 0)
while ( <(i 640)
    set(j 0)

    while ( <(j 480)
        set(x 0)
        set(y 0)
        set(z 1)
        while ( <(z 1E7)
            //print( z )
            
            set(x get(y))
            set(y get(z))
            set(z +(x y))
        )
        set(j +(j 1))
    )
    set(i +(i 1))
)
print("done")
